import styled from '@emotion/styled';

import {
  space,
  layout,
  color,
  flexbox,
  background,
  border,
  position,
  shadow,
  typography,
} from 'styled-system';

export const Box = styled.div`
  ${space}
  ${layout}
  ${color}
  ${flexbox}
  ${background}
  ${border}
  ${position}
  ${shadow}
  ${typography}
`;